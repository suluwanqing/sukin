# 忽略所有文件（强制白名单模式）
*

# 但保留以下内容（根据实际打包目录调整）
!dist/                # 编译后的输出目录
!lib/                 # 或其它编译目录
!package.json         # 必须
!README.md            # 可选
!LICENSE              # 可选
!*.d.ts               # 可选（类型声明文件）